<Window x:Class="CMPADotNetTest.StartupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CM ProtectApp .Net Testing — Configuration"
        Width="500" Height="560" MinHeight="520"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#1A1A2E"
        Foreground="#E0E0E0">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#B0BEC5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0D1B2A"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#0F3460"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Height" Value="30"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#0D1B2A"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#0F3460"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Height" Value="30"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#0D1B2A"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#0F3460"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="#0D1B2A" BorderBrush="#0F3460" BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="22"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="1" Background="#0F3460">
                                                    <Path Data="M2,4 L7,9 L12,4"
                                                          Stroke="#90A4AE" StrokeThickness="1.5" Fill="Transparent"
                                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="8,0,26,0" VerticalAlignment="Center"
                                              TextBlock.Foreground="#E0E0E0"/>
                            <Popup Placement="Bottom" AllowsTransparency="True"
                                   Focusable="False" PopupAnimation="Slide"
                                   IsOpen="{TemplateBinding IsDropDownOpen}">
                                <Grid MinWidth="{TemplateBinding ActualWidth}">
                                    <Border Background="#0D1B2A" BorderBrush="#0F3460" BorderThickness="1">
                                        <ScrollViewer CanContentScroll="True">
                                            <StackPanel IsItemsHost="True"
                                                        KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="#1E3A5F"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#0F3460"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#0F3460"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,6"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="CustomizeButton" TargetType="Button">
            <Setter Property="Background" Value="#1565C0"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,4"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="CM ProtectApp .Net Testing"
                       FontSize="18" FontWeight="Bold"
                       Foreground="#00BCD4" Margin="0,0,0,4"/>
            <TextBlock Text="Enter connection parameters to begin testing"
                       Foreground="#78909C" FontSize="12"/>
        </StackPanel>

        <!-- Form -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" MinHeight="40"/>
                <RowDefinition Height="Auto" MinHeight="40"/>
                <RowDefinition Height="Auto" MinHeight="40"/>
                <RowDefinition Height="Auto" MinHeight="40"/>
                <RowDefinition Height="Auto" MinHeight="40"/>
                <RowDefinition Height="Auto" MinHeight="40"/>
                <RowDefinition Height="Auto" MinHeight="40"/>
                <RowDefinition Height="Auto" MinHeight="40"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Username:"/>
            <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtUsername" VerticalAlignment="Center"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Password:"/>
            <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="PbPassword" VerticalAlignment="Center"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Key Name:"/>
            <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtKeyName" VerticalAlignment="Center"/>

            <Label Grid.Row="3" Grid.Column="0" Content="Duration (sec):"/>
            <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtDuration" VerticalAlignment="Center"/>

            <Label Grid.Row="4" Grid.Column="0" Content="Test Isolation:"/>
            <ComboBox Grid.Row="4" Grid.Column="1" x:Name="CmbTestIsolation" VerticalAlignment="Center">
                <ComboBoxItem Content="Persistent &amp; Dynamic" IsSelected="True"/>
                <ComboBoxItem Content="Persistent only"/>
                <ComboBoxItem Content="Dynamic only"/>
            </ComboBox>

            <Label Grid.Row="5" Grid.Column="0" Content="Key Caching:"/>
            <ComboBox Grid.Row="5" Grid.Column="1" x:Name="CmbKeyCaching" VerticalAlignment="Center">
                <ComboBoxItem Content="None" IsSelected="True"/>
                <ComboBoxItem Content="Memory"/>
                <ComboBoxItem Content="Disk"/>
            </ComboBox>

            <Label Grid.Row="6" Grid.Column="0" Content="Loop Delay:"/>
            <ComboBox Grid.Row="6" Grid.Column="1" x:Name="CmbLoopDelay" VerticalAlignment="Center">
                <ComboBoxItem Content="None" IsSelected="True"/>
                <ComboBoxItem Content="1/10 sec"/>
                <ComboBoxItem Content="1 sec"/>
                <ComboBoxItem Content="5 sec"/>
            </ComboBox>

            <Label Grid.Row="7" Grid.Column="0" Content="Provider Properties:"/>
            <Button Grid.Row="7" Grid.Column="1" x:Name="BtnCustomize"
                    Content="Customize..." Style="{StaticResource CustomizeButton}"
                    HorizontalAlignment="Left" VerticalAlignment="Center"
                    Click="BtnCustomize_Click"/>

            <TextBlock Grid.Row="8" Grid.Column="1" x:Name="TxtError"
                       Foreground="#F44336" FontSize="11"
                       Margin="0,4,0,0" TextWrapping="Wrap"
                       Visibility="Collapsed"/>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Cancel" Style="{StaticResource ActionButton}"
                    Background="#37474F" Margin="0,0,10,0"
                    Click="BtnCancel_Click"/>
            <Button Content="Start Testing" Style="{StaticResource ActionButton}"
                    Background="#00838F"
                    Click="BtnStart_Click"/>
        </StackPanel>
    </Grid>
</Window>
